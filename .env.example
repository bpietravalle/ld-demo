# ============================================
# LaunchDarkly SDKs
# ============================================
# Where to find: LaunchDarkly Dashboard → Settings → Environments → [Your Environment]
# Click "SDK Key" to reveal, copy the Client-side ID and SDK key

# Client-side ID (used by React SDK in browser)
VITE_LD_CLIENT_ID=your-client-side-id

# Server-side SDK key (used by Node SDK in API)
LD_SDK_KEY=sdk-xxx-xxx

# ============================================
# LaunchDarkly REST API (for CLI)
# ============================================
# Where to find: LaunchDarkly Dashboard → Account Settings → Authorization
# Click "Create token" → Give it a name → Select "Writer" role → Create

LD_API_TOKEN=api-xxx-xxx
LD_PROJECT_KEY=default
LD_ENVIRONMENT_KEY=test

# ============================================
# OpenAI (for AI chatbot)
# ============================================
# Where to find: https://platform.openai.com/api-keys
# Click "Create new secret key" → Copy immediately (only shown once)

OPENAI_API_KEY=sk-xxx

# ============================================
# Webhooks (optional)
# ============================================
# Where to find: https://dashboard.ngrok.com/domains
# Get a free static domain, then configure in LD: Integrations → Webhooks

NGROK_DOMAIN=your-domain.ngrok-free.app
# DEBUG_WEBHOOKS=false  # Set to true to log full webhook payloads

# ============================================
# Ports (optional, defaults shown)
# ============================================
# API_PORT=3001
# WEB_PORT=3000
